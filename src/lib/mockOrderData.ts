
import type { Order } from '@/types/pos';
import { allProductsFlat } from './product-generator';

export const generateInitialMockOrders = (): Order[] => {
  // Now returns an empty array, as this should be fetched from a backend
  return [];
};
